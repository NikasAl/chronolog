# 📂 CHRONO_LOG // DEVELOPMENT CONTEXT

**Version:** 0.1.0 (Prototype)
**Stack:** Flutter (Dart), Local Storage
**Theme:** Sci-Fi / Dark / Retro-futurism (Ref: *Dark*, *Tenet*, *Steins;Gate*)

## 1. 🎯 Концепция проекта

Мобильный инструмент для фиксации и анализа статистических аномалий в вероятностных событиях. А также логер жизненных событий, который помогает отмечать планируемые события, оценивать "сопротивление Вселенной" в их реализации и проводить статистический анализ прогноза/результата для выявления статистических аномалий (отклонения от матожидания в сигмах).
**Основная гипотеза:** Намерение наблюдателя и важность события могут влиять на локальное распределение вероятностей (отклонение от матожидания > 3$\sigma$).

**Ключевой цикл (The Loop):**

1. **Intent:** Пользователь задает контекст (Вопрос/Событие) и Важность (1-10).
2. **Observation:** Серия бросков (генерация энтропии).
3. **Analysis:** Вычисление Z-Score (Сигмы). Поиск аномалий.
4. **Reality Check (Feedback):** Подтверждение постфактум — сбылось ли событие?

---

## 2. 🗺 План разработки (Roadmap)

### Phase 1: The Clicker (Prototype) ✅

* [x] Настройка окружения (Arch Linux + Flutter).
* [x] Базовый UI (Dark Theme, Monospace fonts).
* [x] Математическое ядро (расчет Z-Score в реальном времени).
* [x] Визуализация аномалий (цветовое кодирование: Gold/Neon/Red).

### Phase 2: The Oracle (Context & Intent) 🚧 **[CURRENT STAGE]**

* [ ] **Data Model:** Создать классы для хранения События (`ChronosEvent`).
* [ ] **Input UI:** Экран создания события (ввод текста + слайдер важности).
* [ ] **Generation Engine:** Замена одиночного клика на генерацию серии (пакеты по 10/50/100 бросков).
* [ ] **Session Result:** Экран результата серии (с анимацией "сканирования").

### Phase 3: The Archivist (Persistence)

* [ ] Подключение локальной БД (выбор: **Isar** или **SQLite**).
* [ ] Сохранение истории бросков.
* [ ] Экран "Журнал" (Log) со списком прошлых событий.
* [ ] Реализация редактирования (добавление Feedback'а).

### Phase 4: The Analyst (Visualization)

* [ ] Графики распределения (Chart).
* [ ] Подсчет "Sync Rate" (коэффициент совпадения аномалий с реальностью).
* [ ] Геймификация (достижения/ачивки).

---

## 3. 🏗 Архитектура и Структура файлов

Планируемая структура папок `lib/`:

```
lib/
├── main.dart           # Точка входа
├── core/               # Общие константы и утилиты
│   ├── theme.dart      # Цвета (Vantablack, Gold, Neon)
│   └── math_utils.dart # Формулы (Z-Score, Standard Deviation)
├── data/               # Работа с данными
│   ├── models/         # Data Classes (Event, Session)
│   └── database/       # Логика сохранения (Isar/Hive)
├── domain/             # Бизнес-логика (если потребуется)
└── presentation/       # UI (Экраны и виджеты)
    ├── screens/
    │   ├── home_screen.dart    # Дэшборд
    │   ├── input_screen.dart   # Ввод намерения
    │   └── log_screen.dart     # История
    └── widgets/
        ├── anomaly_radar.dart  # Круговой индикатор
        └── glitch_text.dart    # Атмосферный текст

```

---

## 4. 🧬 Модель Данных (Draft)

```dart
enum EventType { decision, prediction, calibration }

class ChronosEvent {
  final String id;          // UUID
  final DateTime timestamp; 
  final String query;       // "Встречу ли я человека?"
  final int importance;     // 1 to 10
  final EventType type;
  
  // Статистика серии
  final int totalSamples;   // n (напр. 100)
  final int positiveHits;   // k (напр. 65)
  final double sigma;       // z-score (напр. 3.0)
  
  // Валидация реальностью
  final bool? isFulfilled;  // null = ожидание, true/false = результат
}

```

---

## 5. 🎨 Design System Specs

* **Background:** `#0A0A0C` (Void Black)
* **Primary (Normal):** `#FFD700` (Gold - Time)
* **Warning (Anomaly > 2σ):** `#00E5FF` (Cherenkov Blue)
* **Critical (Anomaly > 3σ):** `#FF3D00` (Event Horizon Red)
* **Surface/Cards:** `#1E1E24`
* **Font:** Monospace (JetBrains Mono / Roboto Mono preferred)

---

## 6. 📝 Заметки / Ideas

* *Feature Idea:* Добавить звуковой "гул", который меняет тональность в зависимости от текущего отклонения или "счетчик Гейгера".
* *Lore:* 
- Использовать цитаты из сериала Dark или другие связаные с темой приложения при загрузке. 
- Атмосфера создается иллюстрациями. 
- Разработать звуковые эффекты.
- Нужно разработать оформление страницы используя изображения/видео эффекты и промпт-инжиниринг для современных генеративных моделей для создания изображений и коротких видео (~8 сек)
* *Safety:* Нужно убедиться, что `Random()` достаточно случаен. В будущем рассмотреть API квантового генератора.
