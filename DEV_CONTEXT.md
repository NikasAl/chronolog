# 📂 CHRONO_LOG // DEVELOPMENT CONTEXT

**Version:** 0.1.0 (Prototype)
**Stack:** Flutter (Dart), Local Storage
**Theme:** Sci-Fi / Dark / Retro-futurism (Ref: *Dark*, *Tenet*, *Steins;Gate*)

## 1. 🎯 Концепция проекта

Мобильный инструмент для фиксации и анализа статистических аномалий в вероятностных событиях. А также логер жизненных событий, который помогает отмечать планируемые события, оценивать "сопротивление Вселенной" в их реализации и проводить статистический анализ прогноза/результата для выявления статистических аномалий (отклонения от матожидания в сигмах).
**Основная гипотеза:** Намерение наблюдателя и важность события могут влиять на локальное распределение вероятностей (отклонение от матожидания > 3$\sigma$).

**Ключевой цикл (The Loop):**
1.  **Intent:** Пользователь задает контекст (Вопрос/Событие), Важность (1-10) и Размер выборки.
2.  **Observation:** Генерация энтропии (броски монеты) с визуализацией в реальном времени (Сетка + График).
3.  **Analysis:** Вычисление Z-Score (Сигмы). Автоматическое определение вердикта (Норма/Аномалия).
4.  **Persistence:** Авто-сохранение результата.
5.  **Reality Check (Feedback):** (В разработке) Подтверждение постфактум — сбылось ли событие?

---

## 2. 🗺 План разработки (Roadmap)

### Phase 1: The Clicker (Prototype) ✅
- [x] Настройка окружения.
- [x] Базовый UI (Dark Theme, Monospace fonts).
- [x] Математическое ядро (Z-Score).

### Phase 2: The Oracle (Context & Simulation) ✅
- [x] **Data Model:** Класс `ChronosEvent` с поддержкой разных размеров выборки.
- [x] **Input UI:** Экран `InputScreen` (Слайдеры, Чипы, Выбор Samples).
- [x] **Lore System:** Виджет `LoreWrapper` и система всплывающих подсказок.
- [x] **Simulation Engine:** Адаптивная сетка ($\sqrt{N}$), таймер, вибрация.
- [x] **Visualization:** График `SigmaChart` в реальном времени.
- [x] **Result Logic:** Авто-сохранение и панель вердикта (Verdict Panel).

### Phase 3: The Archivist (Persistence) 🚧 **[NEXT STEP]**
- [ ] **Database Setup:** Подключение **Isar** (быстрая NoSQL БД для Flutter).
- [ ] **Repository Layer:** Методы `saveEvent`, `getEvents`, `updateFeedback`.
- [ ] **Dashboard Integration:** Вывод списка последних событий на главном экране.
- [ ] **History Screen:** Экран журнала с фильтрами.

### Phase 4: The Analyst (Visualization & Feedback)
- [ ] Push-уведомления (напоминание проверить исход события).
- [ ] Подсчет "Sync Rate" (коэффициент совпадения аномалий с реальностью).
- [ ] Геймификация.

---

## 3. 🏗 Архитектура и Структура файлов

Текущая структура проекта `lib/`:

```text
lib/
├── main.dart           # Точка входа
├── core/               
│   ├── theme.dart      # AppColors, AppTheme
│   └── strings.dart    # Локализация и Лор
├── data/               
│   └── models/         
│       └── chronos_event.dart
├── presentation/       
    ├── screens/
    │   ├── dashboard_screen.dart    # Главный экран (пока заглушка)
    │   ├── input_screen.dart        # Настройка параметров
    │   └── simulation_screen.dart   # Сетка, График, Вердикт
    └── widgets/
        ├── lore_dialog.dart         # (i) Подсказки
        └── sigma_chart.dart         # CustomPainter графика
```

---

## 4. 🧬 Модель Данных (Draft)

```dart
enum EventType { decision, prediction, calibration }

class ChronosEvent {
  final String id;          
  final DateTime timestamp; 
  final String query;       
  final int importance;     // 1-10
  final int totalSamples;   // 100, 400, 1000
  final EventType type;
  
  // Результаты симуляции
  final int positiveHits;   
  final double sigma;       
  
  // Feedback (заполняется позже)
  final bool? isFulfilled;  
}

```

---

## 5. 🎨 Design System Specs

* **Background:** `#0A0A0C` (Void Black)
* **Primary (Normal):** `#FFD700` (Gold - Time)
* **Warning (Anomaly > 2σ):** `#00E5FF` (Cherenkov Blue)
* **Critical (Anomaly > 3σ):** `#FF3D00` (Event Horizon Red)
* **Surface/Cards:** `#1E1E24`
* **Font:** Monospace (JetBrains Mono / Roboto Mono preferred)

---

## 6. 📝 Заметки / Ideas

- Звук: Добавить "гул" или "треск" счетчика Гейгера при высоких сигмах.
- Использовать цитаты из сериала Dark или другие связаные с темой приложения при загрузке. 
- Атмосфера создается иллюстрациями. 
- Разработать звуковые эффекты.
- Нужно разработать оформление страницы используя изображения/видео эффекты и промпт-инжиниринг для современных генеративных моделей для создания изображений и коротких видео (~8 сек)
- UX: Использовать авто-сохранение результатов симуляции, чтобы пользователь не мог "перегенерировать" плохой результат пока он не станет нужным.
